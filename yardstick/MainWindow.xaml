<Window x:Class="yardstick.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:yardstick"
        mc:Ignorable="d"
        Title="Comparebench Yardstick" Height="450" Width="800" d:DataContext="{d:DesignInstance }">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Text="{Binding ModelName,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window}}">
        </TextBlock>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="{Binding cbScore,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window}}">
        </TextBlock>
        <Button Grid.Column="0" Grid.Row="2" Margin="0,10,0,0" Width="125" Height="25" HorizontalAlignment="Left" Click="SelectCinebenchLocation">Select Cinebench Location</Button>
    </Grid>
</Window>
